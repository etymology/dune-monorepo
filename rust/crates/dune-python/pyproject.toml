[build-system]
requires = ["maturin>=1.13,<2.0"]
build-backend = "maturin"

[project]
name = "dune-rust-audio"
version = "0.1.0"
requires-python = ">=3.12"

[tool.maturin]
module-name = "dune_tension._rust_audio"
python-source = "../../../src"
features = ["pyo3/extension-module"]

