[build-system]
requires = ["maturin>=1.13,<2.0"]
build-backend = "maturin"

[project]
name = "dune-plc-bus"
version = "0.1.0"
requires-python = ">=3.12"

[tool.maturin]
module-name = "dune_plc_bus"
features = ["extension-module"]
