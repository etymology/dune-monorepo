//! Rust → Elm type-bridge codegen entry point. Filled in P10 (will use
//! `elm_rs` derives on shared API types); P1 leaves only the executable
//! shape so CI can be wired now.

fn main() {
    eprintln!("dune-winder-elm-codegen: P1 skeleton — codegen lands in P10.");
}
