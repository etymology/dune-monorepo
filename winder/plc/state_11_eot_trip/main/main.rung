routine state_11_eot_trip/main

uses AbortQueue, ALL_EOT_GOOD, ERROR_CODE, MINUS_X_EOT, MINUS_Y_EOT, MOVE_TYPE, NEXTSTATE  # controller scope
uses PLUS_X_EOT, PLUS_Y_EOT, STATE, STATE_REQUEST, X_axis, X_Y, xz, Y_axis, yz, Z_axis, Z_EOT  # controller scope
uses eot_axes_enabled, eot_entry_ob, eot_entry_sb, eot_manual_ob, eot_manual_sb  # program scope
uses eot_mso_start_ob, eot_mso_start_sb, eot_mso_x, eot_mso_y, eot_mso_z, eot_recover_ready  # program scope
uses eot_recovery_done, eot_stop_x, eot_stop_xy, eot_stop_xz, eot_stop_y, eot_stop_yz  # program scope
uses eot_stop_z, eot_stops_done, STATE11_IND, x_minus_clear_ob, x_minus_clear_sb  # program scope
uses x_minus_cleared, x_minus_jog_active, x_minus_jog_cmd, x_minus_jog_start_ob  # program scope
uses x_minus_jog_start_sb, x_minus_jog_stop, x_minus_jog_timer, x_minus_mrp_cmd  # program scope
uses x_minus_mrp_start_ob, x_minus_mrp_start_sb, x_minus_stop_req, x_minus_timeout, x_minus_trip  # program scope
uses x_plus_clear_ob, x_plus_clear_sb, x_plus_jog_active, x_plus_jog_cmd, x_plus_jog_start_ob  # program scope
uses x_plus_jog_start_sb, x_plus_jog_stop, x_plus_jog_timer, x_plus_stop_req, x_plus_timeout  # program scope
uses x_plus_trip, y_minus_clear_ob, y_minus_clear_sb, y_minus_cleared, y_minus_jog_active  # program scope
uses y_minus_jog_cmd, y_minus_jog_start_ob, y_minus_jog_start_sb, y_minus_jog_stop  # program scope
uses y_minus_jog_timer, y_minus_mrp_cmd, y_minus_mrp_start_ob, y_minus_mrp_start_sb  # program scope
uses y_minus_stop_req, y_minus_timeout, y_minus_trip, y_plus_clear_ob, y_plus_clear_sb  # program scope
uses y_plus_jog_active, y_plus_jog_cmd, y_plus_jog_start_ob, y_plus_jog_start_sb  # program scope
uses y_plus_jog_stop, y_plus_jog_timer, y_plus_stop_req, y_plus_timeout, y_plus_trip  # program scope
uses z_recover_started, z_trip, z_zero_cmd, z_zero_start_ob, z_zero_start_sb  # program scope

# PENDING EDIT in Studio
raw `XIO(ALL_EOT_GOOD)MOV(11,NEXTSTATE)MOV(0,STATE_REQUEST)`

# PENDING EDIT in Studio
raw `EQU(STATE,11)OTE(STATE11_IND)`

# PENDING EDIT in Studio
raw `XIC(STATE11_IND)OSR(eot_entry_sb,eot_entry_ob)`

# PENDING EDIT in Studio
raw `XIC(STATE11_IND)EQU(STATE_REQUEST,11)OSR(eot_manual_sb,eot_manual_ob)`

# PENDING EDIT in Studio
raw `[XIC(eot_entry_ob),XIC(eot_manual_ob)]MOV(0,eot_stop_xy.FLAGS)MOV(0,eot_stop_xz.FLAGS)MOV(0,eot_stop_x.FLAGS)MOV(0,eot_stop_y.FLAGS)MOV(0,eot_stop_z.FLAGS)MOV(0,x_minus_jog_cmd.FLAGS)MOV(0,x_minus_jog_stop.FLAGS)MOV(0,x_minus_mrp_cmd.FLAGS)MOV(0,x_plus_jog_cmd.FLAGS)MOV(0,x_plus_jog_stop.FLAGS)MOV(0,y_minus_jog_cmd.FLAGS)MOV(0,y_minus_jog_stop.FLAGS)MOV(0,y_minus_mrp_cmd.FLAGS)MOV(0,y_plus_jog_cmd.FLAGS)MOV(0,y_plus_jog_stop.FLAGS)MOV(0,z_zero_cmd.FLAGS)MOV(0,eot_mso_x.FLAGS)MOV(0,eot_mso_y.FLAGS)MOV(0,eot_mso_z.FLAGS)RES(x_minus_jog_timer)RES(x_plus_jog_timer)RES(y_minus_jog_timer)RES(y_plus_jog_timer)OTU(x_minus_trip)OTU(x_plus_trip)OTU(y_minus_trip)OTU(y_plus_trip)OTU(z_trip)OTU(x_minus_jog_active)OTU(x_minus_stop_req)OTU(x_minus_cleared)OTU(x_minus_timeout)OTU(x_plus_jog_active)OTU(x_plus_stop_req)OTU(x_plus_timeout)OTU(y_minus_jog_active)OTU(y_minus_stop_req)OTU(y_minus_cleared)OTU(y_minus_timeout)OTU(y_plus_jog_active)OTU(y_plus_stop_req)OTU(y_plus_timeout)OTU(z_recover_started)MOV(10000,x_minus_jog_timer.PRE)MOV(10000,x_plus_jog_timer.PRE)MOV(10000,y_minus_jog_timer.PRE)MOV(10000,y_plus_jog_timer.PRE)MCS(X_Y,eot_stop_xy,All,Yes,10000,Units per sec2,Yes,10000,Units per sec3)MCS(xz,eot_stop_xz,All,Yes,10000,Units per sec2,Yes,10000,Units per sec3)MCS(yz,eot_stop_yz,All,Yes,10000,Units per sec2,Yes,10000,Units per sec3)MAS(X_axis,eot_stop_x,All,Yes,10000,Units per sec2,Yes,10000,Units per sec3)MAS(Y_axis,eot_stop_y,All,Yes,10000,Units per sec2,Yes,10000,Units per sec3)MAS(Z_axis,eot_stop_z,All,Yes,10000,Units per sec2,Yes,10000,Units per sec3)`

# PENDING EDIT in Studio
raw `XIC(STATE11_IND)OTE(AbortQueue)`

# PENDING EDIT in Studio
raw `XIC(STATE11_IND)MOV(0,MOVE_TYPE)MOV(0,STATE_REQUEST)`

# PENDING EDIT in Studio
raw `XIC(STATE11_IND)XIO(MINUS_X_EOT)OTL(x_minus_trip)`

# PENDING EDIT in Studio
raw `XIC(STATE11_IND)XIO(PLUS_X_EOT)OTL(x_plus_trip)`

# PENDING EDIT in Studio
raw `XIC(STATE11_IND)XIO(MINUS_Y_EOT)OTL(y_minus_trip)`

# PENDING EDIT in Studio
raw `XIC(STATE11_IND)XIO(PLUS_Y_EOT)OTL(y_plus_trip)`

# PENDING EDIT in Studio
raw `XIC(STATE11_IND)XIO(Z_EOT)OTL(z_trip)`

# PENDING EDIT in Studio
raw `XIC(STATE11_IND)XIC(eot_stop_xy.DN)XIC(eot_stop_xz.DN)XIC(eot_stop_x.DN)XIC(eot_stop_y.DN)XIC(eot_stop_z.DN)OTE(eot_stops_done)`

# PENDING EDIT in Studio
raw `XIC(eot_stops_done)EQU(X_axis.MotionStatus,0)EQU(Y_axis.MotionStatus,0)OSR(eot_mso_start_sb,eot_mso_start_ob)`

# PENDING EDIT in Studio
raw `XIC(eot_mso_start_ob)MSO(X_axis,eot_mso_x)MSO(Y_axis,eot_mso_y)MSO(Z_axis,eot_mso_z)`

# PENDING EDIT in Studio
raw `XIC(eot_mso_x.DN)XIC(eot_mso_y.DN)XIC(eot_mso_z.DN)OTE(eot_axes_enabled)`

# PENDING EDIT in Studio
raw `XIC(STATE11_IND)XIC(eot_stops_done)XIC(eot_axes_enabled)XIO(X_axis.MoveStatus)XIO(Y_axis.MoveStatus)XIO(Z_axis.MoveStatus)XIO(X_axis.StoppingStatus)XIO(Y_axis.StoppingStatus)XIO(Z_axis.StoppingStatus)OTE(eot_recover_ready)`

# PENDING EDIT in Studio
raw `XIC(STATE11_IND)XIC(eot_recover_ready)XIC(x_minus_trip)XIO(MINUS_X_EOT)XIO(X_axis.StoppingStatus)XIO(x_minus_jog_active)XIO(x_minus_timeout)XIO(x_minus_jog_cmd.IP)XIO(x_plus_trip)XIO(x_plus_stop_req)OSR(x_minus_jog_start_sb,x_minus_jog_start_ob)`

# PENDING EDIT in Studio
raw `XIC(x_minus_jog_start_ob)OTL(x_minus_jog_active)`

# PENDING EDIT in Studio
raw `XIC(x_minus_jog_start_ob)MAJ(X_axis,x_minus_jog_cmd,0,1,Units per sec,1,Units per sec2,1,Units per sec2,Trapezoidal,100,100,% of Time,Disabled,Programmed,0,None)`

# PENDING EDIT in Studio
raw `XIC(x_minus_jog_active)TON(x_minus_jog_timer,10000,0)`

# PENDING EDIT in Studio
raw `XIC(x_minus_trip)XIC(x_minus_jog_active)XIO(x_minus_timeout)XIC(MINUS_X_EOT)OSR(x_minus_clear_sb,x_minus_clear_ob)`

# PENDING EDIT in Studio
raw `XIC(x_minus_clear_ob)OTL(x_minus_cleared)`

# PENDING EDIT in Studio
raw `[XIC(x_minus_clear_ob),XIC(x_minus_jog_active)XIC(x_minus_jog_timer.DN)]OTL(x_minus_stop_req)OTU(x_minus_jog_active)`

# PENDING EDIT in Studio
raw `XIC(x_minus_jog_active)XIC(x_minus_jog_timer.DN)OTL(x_minus_timeout)`

# PENDING EDIT in Studio
raw `XIC(x_minus_stop_req)MAS(X_axis,x_minus_jog_stop,All,Yes,1000,Units per sec2,Yes,1000,Units per sec3)`

# PENDING EDIT in Studio
raw `XIC(x_minus_stop_req)XIC(x_minus_jog_stop.DN)OTU(x_minus_stop_req)`

# PENDING EDIT in Studio
raw `XIC(x_minus_cleared)XIO(X_axis.StoppingStatus)XIC(x_minus_jog_stop.DN)XIO(x_minus_mrp_cmd.IP)OSR(x_minus_mrp_start_sb,x_minus_mrp_start_ob)`

# PENDING EDIT in Studio
raw `XIC(x_minus_mrp_start_ob)MRP(X_axis,x_minus_mrp_cmd,Absolute,Actual,-10)`

# PENDING EDIT in Studio
raw `XIC(x_minus_mrp_cmd.DN)OTU(x_minus_cleared)OTU(x_minus_trip)`

# PENDING EDIT in Studio
raw `XIC(STATE11_IND)XIC(eot_recover_ready)XIC(x_plus_trip)XIO(PLUS_X_EOT)XIO(X_axis.StoppingStatus)XIO(x_plus_jog_active)XIO(x_plus_timeout)XIO(x_plus_jog_cmd.IP)XIO(x_minus_trip)XIO(x_minus_stop_req)XIO(x_minus_cleared)OSR(x_plus_jog_start_sb,x_plus_jog_start_ob)`

# PENDING EDIT in Studio
raw `XIC(x_plus_jog_start_ob)OTL(x_plus_jog_active)`

# PENDING EDIT in Studio
raw `XIC(x_plus_jog_start_ob)MAJ(X_axis,x_plus_jog_cmd,1,1,Units per sec,1,Units per sec2,1,Units per sec2,Trapezoidal,100,100,% of Time,Disabled,Programmed,0,None)`

# PENDING EDIT in Studio
raw `XIC(x_plus_jog_active)TON(x_plus_jog_timer,10000,0)`

# PENDING EDIT in Studio
raw `XIC(x_plus_trip)XIC(x_plus_jog_active)XIO(x_plus_timeout)XIC(PLUS_X_EOT)OSR(x_plus_clear_sb,x_plus_clear_ob)`

# PENDING EDIT in Studio
raw `[XIC(x_plus_clear_ob),XIC(x_plus_jog_active)XIC(x_plus_jog_timer.DN)]OTL(x_plus_stop_req)OTU(x_plus_jog_active)`

# PENDING EDIT in Studio
raw `XIC(x_plus_jog_active)XIC(x_plus_jog_timer.DN)OTL(x_plus_timeout)`

# PENDING EDIT in Studio
raw `XIC(x_plus_stop_req)MAS(X_axis,x_plus_jog_stop,All,Yes,1000,Units per sec2,Yes,1000,Units per sec3)`

# PENDING EDIT in Studio
raw `XIC(x_plus_stop_req)XIC(x_plus_jog_stop.DN)OTU(x_plus_stop_req)OTU(x_plus_trip)`

# PENDING EDIT in Studio
raw `XIC(STATE11_IND)XIC(eot_recover_ready)XIC(y_minus_trip)XIO(MINUS_Y_EOT)XIO(Y_axis.StoppingStatus)XIO(y_minus_jog_active)XIO(y_minus_timeout)XIO(y_minus_jog_cmd.IP)XIO(y_plus_trip)XIO(y_plus_stop_req)OSR(y_minus_jog_start_sb,y_minus_jog_start_ob)`

# PENDING EDIT in Studio
raw `XIC(y_minus_jog_start_ob)OTL(y_minus_jog_active)`

# PENDING EDIT in Studio
raw `XIC(y_minus_jog_start_ob)MAJ(Y_axis,y_minus_jog_cmd,0,1,Units per sec,1,Units per sec2,1,Units per sec2,Trapezoidal,100,100,% of Time,Disabled,Programmed,0,None)`

# PENDING EDIT in Studio
raw `XIC(y_minus_jog_active)TON(y_minus_jog_timer,10000,0)`

# PENDING EDIT in Studio
raw `XIC(y_minus_trip)XIC(y_minus_jog_active)XIO(y_minus_timeout)XIC(MINUS_Y_EOT)OSR(y_minus_clear_sb,y_minus_clear_ob)`

# PENDING EDIT in Studio
raw `XIC(y_minus_clear_ob)OTL(y_minus_cleared)`

# PENDING EDIT in Studio
raw `[XIC(y_minus_clear_ob),XIC(y_minus_jog_active)XIC(y_minus_jog_timer.DN)]OTL(y_minus_stop_req)OTU(y_minus_jog_active)`

# PENDING EDIT in Studio
raw `XIC(y_minus_jog_active)XIC(y_minus_jog_timer.DN)OTL(y_minus_timeout)`

# PENDING EDIT in Studio
raw `XIC(y_minus_stop_req)MAS(Y_axis,y_minus_jog_stop,All,Yes,1000,Units per sec2,Yes,1000,Units per sec3)`

# PENDING EDIT in Studio
raw `XIC(y_minus_stop_req)XIC(y_minus_jog_stop.DN)OTU(y_minus_stop_req)`

# PENDING EDIT in Studio
raw `XIC(y_minus_cleared)XIO(Y_axis.StoppingStatus)XIC(y_minus_jog_stop.DN)XIO(y_minus_mrp_cmd.IP)OSR(y_minus_mrp_start_sb,y_minus_mrp_start_ob)`

# PENDING EDIT in Studio
raw `XIC(y_minus_mrp_start_ob)MRP(Y_axis,y_minus_mrp_cmd,Absolute,Actual,-10)`

# PENDING EDIT in Studio
raw `XIC(y_minus_mrp_cmd.DN)OTU(y_minus_cleared)OTU(y_minus_trip)`

# PENDING EDIT in Studio
raw `XIC(STATE11_IND)XIC(eot_recover_ready)XIC(y_plus_trip)XIO(PLUS_Y_EOT)XIO(Y_axis.StoppingStatus)XIO(y_plus_jog_active)XIO(y_plus_timeout)XIO(y_plus_jog_cmd.IP)XIO(y_minus_trip)XIO(y_minus_stop_req)XIO(y_minus_cleared)OSR(y_plus_jog_start_sb,y_plus_jog_start_ob)`

# PENDING EDIT in Studio
raw `XIC(y_plus_jog_start_ob)OTL(y_plus_jog_active)`

# PENDING EDIT in Studio
raw `XIC(y_plus_jog_start_ob)MAJ(Y_axis,y_plus_jog_cmd,1,1,Units per sec,1,Units per sec2,1,Units per sec2,Trapezoidal,100,100,% of Time,Disabled,Programmed,0,None)`

# PENDING EDIT in Studio
raw `XIC(y_plus_jog_active)TON(y_plus_jog_timer,10000,0)`

# PENDING EDIT in Studio
raw `XIC(y_plus_trip)XIC(y_plus_jog_active)XIO(y_plus_timeout)XIC(PLUS_Y_EOT)OSR(y_plus_clear_sb,y_plus_clear_ob)`

# PENDING EDIT in Studio
raw `[XIC(y_plus_clear_ob),XIC(y_plus_jog_active)XIC(y_plus_jog_timer.DN)]OTL(y_plus_stop_req)OTU(y_plus_jog_active)`

# PENDING EDIT in Studio
raw `XIC(y_plus_jog_active)XIC(y_plus_jog_timer.DN)OTL(y_plus_timeout)`

# PENDING EDIT in Studio
raw `XIC(y_plus_stop_req)MAS(Y_axis,y_plus_jog_stop,All,Yes,1000,Units per sec2,Yes,1000,Units per sec3)`

# PENDING EDIT in Studio
raw `XIC(y_plus_stop_req)XIC(y_plus_jog_stop.DN)OTU(y_plus_stop_req)OTU(y_plus_trip)`

# PENDING EDIT in Studio
raw `XIC(STATE11_IND)XIC(eot_recover_ready)XIC(z_trip)XIO(Z_EOT)XIO(Z_axis.StoppingStatus)XIO(z_recover_started)XIO(z_zero_cmd.IP)OSR(z_zero_start_sb,z_zero_start_ob)`

# PENDING EDIT in Studio
raw `XIC(z_zero_start_ob)OTL(z_recover_started)`

# PENDING EDIT in Studio
raw `XIC(z_zero_start_ob)MAM(Z_axis,z_zero_cmd,0,0,1,Units per sec,1,Units per sec2,1,Units per sec2,Trapezoidal,100,100,% of Time,Disabled,0,0,0,0,0)`

# PENDING EDIT in Studio
raw `XIC(z_zero_cmd.DN)OTU(z_trip)`

# PENDING EDIT in Studio
raw `XIC(STATE11_IND)XIO(x_minus_trip)XIO(x_plus_trip)XIO(y_minus_trip)XIO(y_plus_trip)XIO(z_trip)XIC(PLUS_X_EOT)XIC(MINUS_X_EOT)XIC(PLUS_Y_EOT)XIC(MINUS_Y_EOT)XIC(Z_EOT)XIO(x_minus_jog_active)XIO(x_minus_stop_req)XIO(x_minus_cleared)XIO(x_plus_jog_active)XIO(x_plus_stop_req)XIO(y_minus_jog_active)XIO(y_minus_stop_req)XIO(y_minus_cleared)XIO(y_plus_jog_active)XIO(y_plus_stop_req)XIO(x_minus_jog_cmd.IP)XIO(x_plus_jog_cmd.IP)XIO(x_minus_mrp_cmd.IP)XIO(y_minus_jog_cmd.IP)XIO(y_plus_jog_cmd.IP)XIO(y_minus_mrp_cmd.IP)XIO(z_zero_cmd.IP)OTE(eot_recovery_done)`

# PENDING EDIT in Studio
raw `[XIC(ALL_EOT_GOOD)EQU(STATE,11),XIC(eot_recovery_done)]MOV(0,ERROR_CODE)MOV(0,STATE_REQUEST)MOV(1,NEXTSTATE)`
